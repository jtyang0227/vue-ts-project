import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export default new Vuex.Store({
  // 상태 값
  state: {
  },
  // 상태 변이 함수
  mutations: {
  },
  // 상태 변이를 위하 로직
  actions: {
  },
  // 계산된 값 변환
  modules: {
  },
});
